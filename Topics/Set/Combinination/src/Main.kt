fun solution(first: Set<String>, second: Set<String>): Set<String> {
    return first + second
}
/*
fun main() {
    val first = readln().split(" ").map {it}.toSet()
    val second = readln().split(" ").map {it}.toSet()
    println(solution(first, second))
}
 */